Tween。Change properties by tween equations。var tween = scene.tweens.add({。targets。x。'-=1'。100)'。{ start。function (target。key。tween) { return newValue。ease。'Linear'。duration。repeat。// -1。infinity。yoyo。yoyo。false。Tween to current value + deltaValue。'/=deltaValue'。Tween to a random int value.。Use interpolation to determine the value.。Set the property to value11 when tween started after delay。then tween to value2.。or callback(function(target。Set the property to value0 immediately。Set the property to value0 immediately。then set to value1 when tween started after delay。target。target。　The tween target.。The target property.。The current value of the target property.。The index of the target within the Tween.。The total number of targets in this Tween.。tween。The Tween that invoked this callback.。callback。// timming/callback of each state。function () {}。delay。// tween duration。onActive。current。current。previous。// delay between tween and yoyo。// true to tween backward。// repeat count (-1。// loop count (-1。loop。// delay to next loop。// delay to onComplete callback。// start from current value。'Power2' }。'500'。return value。destX -= 30。The targets the tween is updating.。The time the tween will wait before it first starts。A callback function。From 0 to endValue。From 0 to endValue。From startValue to endValue。scene.tweens.stagger([startValue。with specific ease function。scene.tweens.stagger(endValue。{ease。endValue]。with specific start index。Grid mode. From 0 to endValue.。'center'。Grid mode. From startValue to endValue.。The duration of the tween。The ease function used by the tween。The parameters to go with the ease function (if any)。The time the tween will pause before running a yoyo。The number of times the tween will repeat itself (a value of 1 means the tween will play twice。The time the tween will pause for before starting a repeat. The tween holds in the start state.。boolean - Does the tween reverse itself (yoyo) when it reaches the end。flip Y the GameObject on tween end。Used when the Tween is part of a Timeline。The time the tween will wait before the onComplete event is dispatched once it has completed。-1 for an infinite loop。Does the tween start in a paused state。The properties being tweened by the tween。Tween becomes active within the Tween Manager.。Tween becomes active within the Tween Manager.。function(tween。function(tween。A tween starts.。Callback which fired when tween task updated。Tween completes or is stopped.。A function to call each time the tween yoyos. Called once per property per target.。A function to call each time the tween loops.。A function to call each time the tween repeats. Called once per property per target.。A function to call when the tween is stopped.。A function to call when the tween is paused.。A function to call when the tween is resumed after being paused.。The interpolation function to use if the value given is an array of numbers.。Tween task will not manipulate any property that begins with an underscore.。Cubic.Out。Cubic.Out。Power4。Quintic.Out。Cubic。Circular.Out。Elastic.Out。Back.Out。Bounce.Out。Bounce.easeIn。Cubic.easeOut。Cubic.easeInOut。Sine.easeInOut。